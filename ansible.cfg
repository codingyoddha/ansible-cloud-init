[defaults]
# Automatically target localhost without needing a hosts file
inventory = localhost,
# Use the local connection plugin by default (faster, no SSH)
transport = local
# Silence the "implicit localhost" warning
localhost_warning = False
# Silence host pattern mismatch warnings
display_skipped_hosts = False